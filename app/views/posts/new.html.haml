.d-flex.justify-content-center.mt-5
  .card.w-50
    .card-body
      %h2 新規作成
      = form_with(model: @post) do |f|
        .field
          = f.file_field(:image)
        .field.mt-2
          = f.text_area(:comment, rows: 10)
        .actions.mt-3
          = f.submit
